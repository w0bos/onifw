import os
import sys
import time








class Updater:
    def __init__(self, repo_url, local_version):
            self.repo_url = repo_url
            self.local_version = local_version

            
            os.system("echo curl rawfile")














